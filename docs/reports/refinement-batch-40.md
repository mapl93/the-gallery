# Component Refinement Batch 40

Status: Certificate of Authenticity refined and ready for human review

Date: 2026-07-14

Components: Certificate of Authenticity

## Outcome

Certificate's dossier, standards research, passive record/verification decision,
contract, canonical CSS, shared Exhibit/Studio renderer, Studio metadata, MDX,
Shopify Liquid/locales, generated adapters, browser evidence, open boundary and
individual audit are reconciled. The contract remains `pilot`; no component was
promoted to `stable`.

`site/dist` was not rebuilt or modified. Button remains the only human-approved
stable component.

## Safe Refinement

- One self-contained native article is named by its visible contextual title.
- Target-owned metadata uses native grouped `dl`/`dt`/`dd` semantics and omits
  absent values without empty wrappers.
- Verification is an ordinary descriptive native link rendered only from a real
  target destination; optional media never carries the only purpose.
- The neutral source creates no credential status, issuer trust, proof,
  signature, QR service, authenticity claim or network request.
- A vendor/attribution is no longer presented as a signature in Shopify.
- One intrinsic card container drives the one/two-column metadata layout.
- Complete semantic typography, source-strength host isolation, focus, forced
  colors and extreme reflow containment now live in canonical CSS.
- Exhibit and Studio share one renderer, fixture and exact initial markup.
- Shopify localizes native details, accepts explicit optional target inputs,
  removes the synthetic external QR and reports implemented/ready.

## Browser Evidence Summary

- Exact Exhibit/Studio initial `outerHTML` parity holds in Mobile, Tablet,
  Desktop and XL; clean component markup is `1,419` characters.
- The card is `295px`/one column on Mobile and `480px`/two columns from Tablet
  through XL, with no standard-viewport page overflow.
- A constrained `320px` host switches independently to one column. Public
  optionals can reduce to the required title without empty public wrappers.
- Sequential link focus has a `2px` primary outline and `3px` offset.
- Five localized/long RTL details and unbroken identifiers stay contained.
- Isolated 200% zoom passes. An even stricter `195px` reflow probe exposed and
  then verified the fluid verification-media fix.
- Light/dark contrast passes; forced-color border/rule/focus and reduced-motion
  zero-motion behavior pass. Final console inspection has no errors or warnings.
- Four before, eight viewport-after and five special-state images live under
  `output/playwright/refinement-batch-40/`.

## Performance Budgets

| Surface | Measured gzip | Ceiling | Result |
| --- | ---: | ---: | --- |
| Storytelling CSS | `4,220 B` | `4.2 KiB` | pass (`80 B` headroom; `+308 B` from Batch 39) |
| Shared neutral runtime | `10,492 B` | `8 KiB` | existing exception (`2,300 B` over; no Certificate runtime) |
| Neutral Web components CSS | `66,025 B` | `64 KiB` | gap (`489 B` over; `+336 B` from Batch 39) |

The global CSS ceiling remains unchanged. The overage is an explicit program gap
for later shared-bundle optimization or owner decision; semantic typography,
focus, target boundaries and extreme reflow containment were not removed to
manufacture a pass.

## Human Review Queue

Review the archival surface, decorative border/rule, centered hierarchy,
heading/accent pairing, metadata density/rhythm, `30rem` measure, intrinsic
one/two-column threshold, fluid verification-media maximum and full-width link
focus target. Confirm that alignment, columns, border controls, QR size,
print/download mode, issuer/proof fields and verification status stay outside the
v1 public API.

Production certificate records, verification destinations, issuer/proof/
revocation semantics and authenticity guarantees remain a separate explicit
owner/target architecture decision. The passive component does not block on
that decision because it truthfully omits verification without a destination.

## Validation

Registry/docs, tokens, 183 contracts, 183 Studios, Neutral Web, Shopify and
Webflow outputs, mandatory Shopify docs/official validation, structural/static/
parity audits, browser states/preferences/reflow, source/generated identity,
deterministic gzip, global refinement audit, temporary site build, diff checks,
console cleanliness and `site/dist` cleanliness form the final gate.

## Program Progress

After regeneration the program should contain 183 components, 108 dependency
edges, 81 dossiers and 70 components ready for human review. Certificate is
`human-review-ready`; it remains `pilot` and human review is still pending.
