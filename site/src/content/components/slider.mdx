## Overview

Single and dual-handle range slider with value display.

## Presentation

Single and range modes accept `default`, `error`, `success`, and `warning` through `data-validation` without conflating validation with geometry.

<Preview html={"<div class=\"slider tg-preview-frame--sm\"><label class=\"slider__label\" for=\"volume-range\"><span>Volume</span><span class=\"slider__value\">60</span></label><div class=\"slider__track\"><input id=\"volume-range\" name=\"volume\" type=\"range\" class=\"slider__input\" min=\"0\" max=\"100\" step=\"1\" value=\"60\" /></div></div>"} label="Slider / Range" />

### Single

<Preview html={"<div class=\"slider tg-preview-frame--sm\"><label class=\"slider__label\" for=\"single-range\"><span>Opacity</span><span class=\"slider__value\">50</span></label><div class=\"slider__track\"><input id=\"single-range\" name=\"opacity\" type=\"range\" class=\"slider__input\" min=\"0\" max=\"100\" step=\"1\" value=\"50\" /></div></div>"} label="Single" />

### Range

<Preview html={"<div class=\"slider tg-preview-frame--sm\"><div class=\"slider__label\" id=\"price-range-label\"><span>Price range</span><span class=\"slider__value\">100–400</span></div><div class=\"range-slider\" role=\"group\" aria-labelledby=\"price-range-label\"><div class=\"range-slider__track\"></div><div class=\"range-slider__fill\"></div><input class=\"range-slider__input\" data-range-lower name=\"priceMin\" type=\"range\" min=\"0\" max=\"500\" step=\"10\" value=\"100\" aria-label=\"Minimum price\" /><input class=\"range-slider__input\" data-range-upper name=\"priceMax\" type=\"range\" min=\"0\" max=\"500\" step=\"10\" value=\"400\" aria-label=\"Maximum price\" /></div></div>"} label="Range" />

## Usage Guidelines

<DoDont>
  <Do>Always pair form inputs with visible labels for accessibility.</Do>
  <Dont>Don't rely on placeholder text as the only label — it disappears on input.</Dont>
</DoDont>

## Accessibility

<Callout type="tip">
  Use native `input type="range"` for expected keyboard and assistive technology behavior.
</Callout>

- Provide a visible label or equivalent accessible name.
- Expose `min`, `max`, `step`, and current value.
- Keep visible value text synchronized with the input value.
- For dual-handle ranges, keep lower and upper values ordered and announce each handle clearly.
- In range mode, give the lower and upper native inputs distinct form names. Their submitted values and reset behavior remain authoritative.
- Framework adapters may control the native values and consume `input`/`change`; they should not add a mirrored hidden range value.

## References

<PropTable>
  <tr><td>`.slider`</td><td>class</td><td>—</td><td>Single-value slider wrapper</td></tr>
  <tr><td>`data-validation`</td><td>attribute</td><td>`default`</td><td>`error`, `success`, or `warning` validation family</td></tr>
  <tr><td>`.slider__label`</td><td>element</td><td>—</td><td>Label and value row</td></tr>
  <tr><td>`.slider__value`</td><td>element</td><td>—</td><td>Formatted current value</td></tr>
  <tr><td>`.slider__track`</td><td>element</td><td>—</td><td>Optional range input positioning wrapper</td></tr>
  <tr><td>`.slider__input`</td><td>element</td><td>`type="range"`</td><td>Native single-value range input</td></tr>
  <tr><td>`.range-slider`</td><td>variant</td><td>—</td><td>Dual-handle range slider wrapper</td></tr>
  <tr><td>`.range-slider__track`</td><td>element</td><td>—</td><td>Dual-handle range track</td></tr>
  <tr><td>`.range-slider__fill`</td><td>element</td><td>—</td><td>Selected range fill between handles</td></tr>
  <tr><td>`.range-slider__input`</td><td>element</td><td>`type="range"`</td><td>Native range handle input</td></tr>
  <tr><td>`data-range-lower`</td><td>attribute</td><td>required in range mode</td><td>Identifies the stable lower value owner</td></tr>
  <tr><td>`data-range-upper`</td><td>attribute</td><td>required in range mode</td><td>Identifies the stable upper value owner</td></tr>
</PropTable>
