## Overview

Groups multiple buttons with shared borders.

<Preview html={"<div class=\"btn-group\"><button class=\"btn btn--outline\">Left</button><button class=\"btn btn--outline\">Center</button><button class=\"btn btn--outline\">Right</button></div>"} label="Button Group" />

## Usage Guidelines

<DoDont>
  <Do>Use this component consistently across your theme for a cohesive UI.</Do>
  <Dont>Don't override the component's built-in states with custom CSS — use the provided variants instead.</Dont>
</DoDont>

## Accessibility

<Callout type="info">
  Primitive components are building blocks. They're referenced by higher-level components throughout the system.
</Callout>

- Use `<button>` elements for actions and `<a>` for navigation. Never use `<div>` with a click handler.

## Dependencies

This component uses:

- [Button](/components/button)

## API Reference

<PropTable>
  <tr><td>`.btn-group`</td><td>class</td><td>—</td><td>Base button group styles</td></tr>
</PropTable>
