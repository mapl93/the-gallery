## Overview

Form layout with sections, rows, columns, actions, and error summary.

<Preview html={"<form class=\"form\" style=\"max-width:400px;\"><div class=\"field\"><label class=\"field__label\">Name</label><input class=\"input__field\" type=\"text\" placeholder=\"Your name\" /></div><div class=\"field\"><label class=\"field__label\">Email</label><input class=\"input__field\" type=\"email\" placeholder=\"you@example.com\" /></div><button class=\"btn\" type=\"submit\">Submit</button></form>"} label="Form" />

## Usage Guidelines

<DoDont>
  <Do>Always pair form inputs with visible labels for accessibility.</Do>
  <Dont>Don't rely on placeholder text as the only label — it disappears on input.</Dont>
</DoDont>

## Accessibility

<Callout type="tip">
  Use the `field-wrapper` component to ensure consistent spacing and label alignment across all form fields.
</Callout>

- Associate every input with a `<label>` using matching `for`/`id` attributes.

## Dependencies

This component uses:

- [Field Wrapper](/components/field-wrapper)
- [Button](/components/button)

## API Reference

<PropTable>
  <tr><td>`.form`</td><td>class</td><td>—</td><td>Base form styles</td></tr>
</PropTable>
