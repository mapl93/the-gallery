## Overview

A modal side-panel composition for supporting tasks that preserve page context. Drawer owns physical side presentation and scroll partition; each target owns true modality, focus, inertness, and lifecycle.

<Preview html={"<div class=\"tg-preview-stage tg-preview-stage--overlay\"><button class=\"btn\" type=\"button\" data-preview-trigger aria-expanded=\"true\">Open drawer</button><div class=\"drawer-overlay is-open tg-preview-overlay-layer\" aria-hidden=\"false\"><aside class=\"drawer is-open tg-preview-drawer-layer\" role=\"dialog\" aria-modal=\"true\" aria-labelledby=\"drawer-preview-title\"><div class=\"drawer__header\"><h2 class=\"drawer__title\" id=\"drawer-preview-title\">Collection notes</h2><button class=\"close-btn drawer__close\" type=\"button\" data-preview-close aria-label=\"Close drawer\"><svg class=\"close-btn__icon\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" aria-hidden=\"true\"><path d=\"M18 6 6 18M6 6l12 12\"/></svg></button></div><div class=\"drawer__body\"><p>This vessel was formed in three stages and fired at high temperature.</p></div><div class=\"drawer__footer\"><button class=\"btn btn--full\" type=\"button\" data-preview-close>Done</button></div></aside></div></div>"} label="Drawer" layout="overlay" interaction={{"selector":".drawer-overlay, .drawer","toggle":"is-open","triggerLabel":"Open Drawer","triggerSelector":"[data-preview-trigger]","closeSelector":"[data-preview-close]","startVisible":true}} />

## Presentation

Drawer enters from the right by default. `left` changes its resting and open transform while preserving the same anatomy and modal behavior requirements.

| Element     | Class             | Purpose                                  |
|-------------|-------------------|------------------------------------------|
| Overlay     | `.drawer-overlay` | Darkened backdrop layer                  |
| Container   | `.drawer`         | Fixed side panel                         |
| Header      | `.drawer__header` | Required title and close region          |
| Title       | `.drawer__title`  | Required visible accessible name         |
| Close       | `.drawer__close`  | Required canonical Close Button          |
| Body        | `.drawer__body`   | Scrollable primary content region        |
| Footer      | `.drawer__footer` | Optional action or summary region        |

## Usage Guidelines

<DoDont>
  <Do>Use Drawer for supporting tasks that benefit from preserving page context.</Do>
  <Dont>Don't nest another modal overlay inside a Drawer.</Dont>
</DoDont>

## Accessibility

<Callout type="warning">
  Drawer CSS handles presentation, not focus trapping. The target must make outside content inert, contain focus, close on Escape, and restore focus before asserting modal semantics.
</Callout>

- Trap focus inside the overlay while open. Return focus to the trigger on close.
- Close on `Escape` key press.
- Provide `role="dialog"`, `aria-modal="true"`, and a programmatic name when page interaction is blocked.

## Visual Customization

`--space-drawer-width` starts at 400px and remains bounded by available width minus the responsive viewport gap (factor 1 initially). Header, body, footer and close-gap factors each start at 1 and can be customized independently. Divider width starts at 1px. Full dynamic viewport height and complete offscreen displacement remain structural; physical Right/Left is still the accepted placement property.

Studio and Exhibit translate the viewport bound to their local preview width. They use the same preferred-width/gap tokens instead of fixed demo widths, so customization is visible without changing the component placement contract.

The backdrop tint is `--color-overlay` (black initially), composed with the existing unitless `--opacity-overlay`; both inputs are editable. An alpha tint multiplies with scrim density. Surface, shadow, title family/size/line/weight, transition duration/curves and layer levels keep shared semantic roles. Close Button and Button continue to own their internal appearance.

Spacing factors multiply `--space-layout-element-gap`, preserving theme/viewport response without repeating derived values at each breakpoint. The factor is unitless: base 32px × 0.75 = 24px. Body/header/footer positioning, scroll partition, open/closed transforms, and forced-color system boundaries remain component mechanics. Color or geometry customization is not a modality or accessibility certification.

## References

<PropTable>
  <tr><td>`title`</td><td>string</td><td>required</td><td>Visible localized title used by `aria-labelledby`</td></tr>
  <tr><td>`content`</td><td>slot</td><td>required</td><td>Primary content in the independently scrollable body</td></tr>
  <tr><td>`dismissAction`</td><td>slot</td><td>required</td><td>Native action composed with Close Button</td></tr>
  <tr><td>`dismissLabel`</td><td>string</td><td>required</td><td>Contextual accessible name for dismissal</td></tr>
  <tr><td>`footer`</td><td>slot</td><td>optional</td><td>Persistent summary or Button actions</td></tr>
  <tr><td>`placement`</td><td>enum</td><td>`default`</td><td>`default` right side or `left`</td></tr>
  <tr><td>`open`</td><td>boolean</td><td>`false`</td><td>Applies `.is-open` to overlay and drawer</td></tr>
  <tr><td>`.drawer-overlay`</td><td>class</td><td>—</td><td>Backdrop with overlay opacity</td></tr>
  <tr><td>`.drawer`</td><td>class</td><td>—</td><td>Base right-side drawer panel</td></tr>
  <tr><td>`.drawer--left`</td><td>class</td><td>—</td><td>Left-side placement variant</td></tr>
  <tr><td>`.drawer__header`</td><td>class</td><td>—</td><td>Header section</td></tr>
  <tr><td>`.drawer__title`</td><td>class</td><td>—</td><td>Visible title styling</td></tr>
  <tr><td>`.drawer__close`</td><td>class</td><td>—</td><td>Close control</td></tr>
  <tr><td>`.drawer__body`</td><td>class</td><td>—</td><td>Scrollable body section</td></tr>
  <tr><td>`.drawer__footer`</td><td>class</td><td>—</td><td>Footer section</td></tr>
  <tr><td>`.is-open`</td><td>state class</td><td>—</td><td>Applied to overlay and drawer when visible</td></tr>
</PropTable>
