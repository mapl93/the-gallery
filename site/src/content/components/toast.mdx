## Overview

Notification toast. 4 feedback variants.

<Preview html={"<div class=\"toast is-visible\" role=\"alert\" style=\"position:relative;\"><span class=\"toast__icon\">✓</span><div class=\"toast__content\"><p class=\"toast__title\">Saved</p><p class=\"toast__message\">Your changes have been saved.</p></div><button class=\"toast__close\" aria-label=\"Dismiss\">&times;</button></div>"} label="Toast" interaction={{"selector":".toast","toggle":"is-visible","triggerLabel":"Show Toast","startVisible":false}} />

## Variants

### Info

<Preview html={"<div class=\"toast toast--info is-visible\" role=\"alert\" style=\"position:relative;\"><div class=\"toast__content\"><p class=\"toast__message\">Info toast.</p></div></div>"} label="Info" />

### Success

<Preview html={"<div class=\"toast toast--success is-visible\" role=\"alert\" style=\"position:relative;\"><div class=\"toast__content\"><p class=\"toast__message\">Success toast.</p></div></div>"} label="Success" />

### Error

<Preview html={"<div class=\"toast toast--error is-visible\" role=\"alert\" style=\"position:relative;\"><div class=\"toast__content\"><p class=\"toast__message\">Error toast.</p></div></div>"} label="Error" />

### Warning

<Preview html={"<div class=\"toast toast--warning is-visible\" role=\"alert\" style=\"position:relative;\"><div class=\"toast__content\"><p class=\"toast__message\">Warning toast.</p></div></div>"} label="Warning" />

## Usage Guidelines

<DoDont>
  <Do>Use semantic HTML (section, nav, aside) alongside the layout classes.</Do>
  <Dont>Don't nest layout overlays (modal inside drawer, etc.) — it creates confusing z-index stacking.</Dont>
</DoDont>

## Accessibility

<Callout type="warning">
  Layout components handle z-index and focus trapping. Test keyboard navigation before shipping.
</Callout>

- Use `role="alert"` or `aria-live="assertive"` for important notifications.

## API Reference

<PropTable>
  <tr><td>`.toast`</td><td>class</td><td>—</td><td>Base toast styles</td></tr>
  <tr><td>`.toast--info`</td><td>modifier</td><td>—</td><td>Info variant</td></tr>
  <tr><td>`.toast--success`</td><td>modifier</td><td>—</td><td>Success variant</td></tr>
  <tr><td>`.toast--error`</td><td>modifier</td><td>—</td><td>Error variant</td></tr>
  <tr><td>`.toast--warning`</td><td>modifier</td><td>—</td><td>Warning variant</td></tr>
</PropTable>
