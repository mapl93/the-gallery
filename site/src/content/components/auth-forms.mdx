## Overview

Login and registration forms with social auth and dividers.

<Preview html={"<div class=\"auth\" style=\"max-width:380px;\"><div class=\"auth__header\"><h2 class=\"auth__title\">Sign In</h2><p class=\"auth__subtitle\">Welcome back</p></div><form class=\"auth__form\"><div class=\"field\"><label class=\"field__label\">Email</label><input class=\"input__field\" type=\"email\" placeholder=\"your@email.com\" /></div><div class=\"field\"><label class=\"field__label\">Password</label><input class=\"input__field\" type=\"password\" placeholder=\"••••••••\" /></div><button class=\"btn\" style=\"width:100%;margin-top:12px;\">Sign In</button></form></div>"} label="Auth Forms (Login / Register)" />

## Usage Guidelines

<DoDont>
  <Do>Pre-fill fields when possible (e.g., address from previous orders) to reduce friction.</Do>
  <Dont>Don't expose sensitive account data without authentication.</Dont>
</DoDont>

## Accessibility

<Callout type="warning">
  Account components require Shopify customer authentication. They render only for logged-in customers.
</Callout>

- Associate every input with a `<label>` using matching `for`/`id` attributes.

## Dependencies

This component uses:

- [Input](/components/input)
- [Button](/components/button)
- [Form](/components/form)

## API Reference

<PropTable>
  <tr><td>`.auth`</td><td>class</td><td>—</td><td>Base auth forms (login / register) styles</td></tr>
</PropTable>
