## Overview

Side panel overlay. Left or right.

<Preview html={"<div class=\"drawer-overlay is-open\" style=\"position:relative;min-height:300px;background:rgba(0,0,0,0.2);\"><div class=\"drawer is-open\" style=\"position:absolute;right:0;top:0;bottom:0;width:320px;\"><div class=\"drawer__header\"><h2>Drawer</h2><button class=\"drawer__close\">&times;</button></div><div class=\"drawer__body\"><p>Drawer content here.</p></div><div class=\"drawer__footer\"><button class=\"btn\">Done</button></div></div></div>"} label="Drawer" interaction={{"selector":".drawer-overlay, .drawer","toggle":"is-open","triggerLabel":"Open Drawer","startVisible":true}} />

## Usage Guidelines

<DoDont>
  <Do>Use semantic HTML (section, nav, aside) alongside the layout classes.</Do>
  <Dont>Don't nest layout overlays (modal inside drawer, etc.) — it creates confusing z-index stacking.</Dont>
</DoDont>

## Accessibility

<Callout type="warning">
  Layout components handle z-index and focus trapping. Test keyboard navigation before shipping.
</Callout>

- Trap focus inside the overlay while open. Return focus to the trigger on close.
- Close on `Escape` key press.

## API Reference

<PropTable>
  <tr><td>`.drawer`</td><td>class</td><td>—</td><td>Base drawer styles</td></tr>
</PropTable>
