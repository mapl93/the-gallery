# Refinement Batch 160 — Glaze Guide Decision Reconciliation

Date: 2026-08-11

Component: R2 `glaze-guide`

Result: ready for explicit human review; remains `pilot`

## Accepted Direction

Owner decisions 54 and 56 confirm `R2-A`: one passive target-owned collection
of named glaze samples with optional explicitly featured reference detail.
Featured content never creates selected option state. Every sample retains a
complete visible target-supplied name independent of color or imagery.

When glaze is a purchasable option, the product target composes canonical Radio
or Variant Selector outside R2 and owns eligibility, availability, validation
and selected-variant synchronization. R2 exposes no selectable mode.

## Reconciliation Result

The existing `0.2.0` pilot already matches the accepted direction:

- conditional named section or generic root;
- required non-empty `ul > li > figure > figcaption` sample collection;
- text-complete identity and optional target-formatted codes;
- optional separately named featured article with contextual media and native
  description-list facts;
- no buttons, widget roles, selection attributes, focusability, hover/click
  response, synchronization or neutral runtime;
- one renderer/fixture shared by Exhibit and Studio;
- intrinsic layout and strict optional/invalid omission.

No source behavior change or browser session was necessary. The current R2 CSS
slice retains SHA-256
`2980c7e8d73776044adfb4ba746b81ea4412fa6d7e15ac496c6f5eb4f1ca767e`,
exactly matching Batch 111. That evidence contains before/after captures, four
paired natural viewports, omission/minimal/RTL/extreme/zoom/text-spacing modes,
dark, forced colors, reduced motion, exact DOM/style parity, zero overflow, AA
contrast and zero console/page errors. Its resource lifecycle remains clean.

## Current Performance And Targets

- R2 CSS: `4,066 B` raw / `1,009 B` gzip, byte-identical to Batch 111;
- Ceramics CSS: `33,605 B` raw / `4,847 B` gzip against `5,427 B`, leaving
  `580 B` after later R-family refinements;
- R2 neutral runtime/assets/listeners/observers/timers/requests: zero;
- Webflow and Shopify Ceramics CSS: source-identical;
- Shopify passive R2 remains planned; product selection stays outside R2.

## Remaining Gates

- production record, formula/firing/compatibility and reviewed claim proof;
- production color/media identity, management, alternatives and disclosure;
- first passive Shopify consumer and editor mapping;
- owner approval of final sample/detail geometry and responsive visuals;
- R2-specific design evidence or explicit repository-render approval;
- explicit human stability review.

No `stable` promotion is authorized.
