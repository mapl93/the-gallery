## Overview

Drag-and-drop file upload zone with preview list.

## Presentation

File Upload supports `default`, `error`, `success`, and `warning` validation families. Drag-over and focus retain the active family.

<Preview html={"<div class=\"file-upload tg-preview-frame--md\"><input class=\"file-upload__input\" type=\"file\" id=\"demo-upload\" accept=\"image/png,image/jpeg\" /><label for=\"demo-upload\"><span class=\"file-upload__text\">Click to upload or drag and drop</span><span class=\"file-upload__hint\">PNG or JPG up to 10 MB</span></label></div>"} label="File Upload / Dropzone" />

## Usage Guidelines

<DoDont>
  <Do>Always pair form inputs with visible labels for accessibility.</Do>
  <Dont>Don't rely on placeholder text as the only label — it disappears on input.</Dont>
</DoDont>

## Accessibility

<Callout type="tip">
  Drag and drop is an enhancement. The native file input remains the primary upload path.
</Callout>

- Expose accepted file types and size limits in visible helper text.
- Announce validation errors and selected file names.
- Keep previews synchronized with the current file list.
- Provide useful alt text or file names for image previews.

## References

<PropTable>
  <tr><td>`.file-upload`</td><td>class</td><td>—</td><td>Dropzone surface and positioning context</td></tr>
  <tr><td>`.file-upload--error`</td><td>modifier</td><td>—</td><td>Error validation family</td></tr>
  <tr><td>`.file-upload--success`</td><td>modifier</td><td>—</td><td>Success validation family</td></tr>
  <tr><td>`.file-upload--warning`</td><td>modifier</td><td>—</td><td>Warning validation family</td></tr>
  <tr><td>`.file-upload--dragover`</td><td>modifier</td><td>—</td><td>Active drag-over state</td></tr>
  <tr><td>`.file-upload__input`</td><td>element</td><td>`type="file"`</td><td>Native file input</td></tr>
  <tr><td>`.file-upload__icon`</td><td>element</td><td>—</td><td>Optional upload icon</td></tr>
  <tr><td>`.file-upload__text`</td><td>element</td><td>—</td><td>Primary upload instruction</td></tr>
  <tr><td>`.file-upload__hint`</td><td>element</td><td>—</td><td>Format, size, or helper text</td></tr>
  <tr><td>`.file-upload__preview`</td><td>element</td><td>—</td><td>Preview list for selected files</td></tr>
  <tr><td>`.file-upload__thumb`</td><td>element</td><td>—</td><td>Preview thumbnail</td></tr>
</PropTable>
