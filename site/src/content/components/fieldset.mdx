## Overview

Styled fieldset group with legend.

<Preview html={"<fieldset class=\"fieldset\" style=\"max-width:400px;\"><legend class=\"fieldset__legend\">Shipping Address</legend><div class=\"field\"><label class=\"field__label\">Street</label><input class=\"input__field\" type=\"text\" /></div></fieldset>"} label="Fieldset" />

## Usage Guidelines

<DoDont>
  <Do>Always pair form inputs with visible labels for accessibility.</Do>
  <Dont>Don't rely on placeholder text as the only label — it disappears on input.</Dont>
</DoDont>

## Accessibility

<Callout type="tip">
  Use the `field-wrapper` component to ensure consistent spacing and label alignment across all form fields.
</Callout>

- Associate every input with a `<label>` using matching `for`/`id` attributes.

## API Reference

<PropTable>
  <tr><td>`.fieldset`</td><td>class</td><td>—</td><td>Base fieldset styles</td></tr>
</PropTable>
