## Overview

One-time code input with individual character cells.

## Presentation

Pin Input supports `default`, `error`, `success`, and `warning` validation families across every cell in the group.

<Preview html={"<div class=\"pin-input\"><input class=\"pin-input__field pin-input__field--filled\" type=\"text\" inputmode=\"numeric\" autocomplete=\"one-time-code\" maxlength=\"1\" value=\"4\" aria-label=\"Digit 1\" /><input class=\"pin-input__field pin-input__field--filled\" type=\"text\" inputmode=\"numeric\" maxlength=\"1\" value=\"2\" aria-label=\"Digit 2\" /><input class=\"pin-input__field\" type=\"text\" inputmode=\"numeric\" maxlength=\"1\" aria-label=\"Digit 3\" /><input class=\"pin-input__field\" type=\"text\" inputmode=\"numeric\" maxlength=\"1\" aria-label=\"Digit 4\" /></div>"} label="Pin Input / OTP" />

## Usage Guidelines

<DoDont>
  <Do>Always pair form inputs with visible labels for accessibility.</Do>
  <Dont>Don't rely on placeholder text as the only label — it disappears on input.</Dont>
</DoDont>

## Accessibility

<Callout type="tip">
  Pin Input owns the cell layout; focus movement, paste distribution, and validation are target-owned behavior.
</Callout>

- Provide a visible group label for the full one-time-code input.
- Use `autocomplete="one-time-code"` when capturing verification codes.
- Keep every cell keyboard reachable and support paste for full-code entry.

## References

<PropTable>
  <tr><td>`.pin-input`</td><td>class</td><td>—</td><td>Wrapper that lays out individual one-time-code cells</td></tr>
  <tr><td>`.pin-input--error`</td><td>modifier</td><td>—</td><td>Error validation family</td></tr>
  <tr><td>`.pin-input--success`</td><td>modifier</td><td>—</td><td>Success validation family</td></tr>
  <tr><td>`.pin-input--warning`</td><td>modifier</td><td>—</td><td>Warning validation family</td></tr>
  <tr><td>`.pin-input__field`</td><td>element</td><td>`type="text"`</td><td>Single character native input cell</td></tr>
  <tr><td>`.pin-input__field--filled`</td><td>state</td><td>—</td><td>Cell state after a character has been entered</td></tr>
</PropTable>
