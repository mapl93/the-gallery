## Overview

Multi-line text input. Extends input component.

<Preview html={"<div style=\"max-width:400px;\"><label class=\"input__label\" style=\"display:block;margin-bottom:4px;\">Message</label><textarea class=\"textarea__field\" rows=\"3\" placeholder=\"Write something…\"></textarea></div>"} label="Textarea" />

## Usage Guidelines

<DoDont>
  <Do>Use this component consistently across your theme for a cohesive UI.</Do>
  <Dont>Don't override the component's built-in states with custom CSS — use the provided variants instead.</Dont>
</DoDont>

## Accessibility

<Callout type="info">
  Primitive components are building blocks. They're referenced by higher-level components throughout the system.
</Callout>

- Associate every input with a `<label>` using matching `for`/`id` attributes.

## Dependencies

This component uses:

- [Input](/components/input)

## API Reference

<PropTable>
  <tr><td>`.textarea__field`</td><td>class</td><td>—</td><td>Base textarea styles</td></tr>
</PropTable>
