## Overview

Universal label + input + help/error wrapper for all form fields.

<Preview html={"<div class=\"field\" style=\"max-width:320px;\"><label class=\"field__label\">Email</label><input class=\"input__field\" type=\"email\" placeholder=\"you@example.com\" /></div>"} label="Field Wrapper" />

## Usage Guidelines

<DoDont>
  <Do>Always pair form inputs with visible labels for accessibility.</Do>
  <Dont>Don't rely on placeholder text as the only label — it disappears on input.</Dont>
</DoDont>

## Accessibility

<Callout type="tip">
  Use the `field-wrapper` component to ensure consistent spacing and label alignment across all form fields.
</Callout>

- Associate every input with a `<label>` using matching `for`/`id` attributes.

## API Reference

<PropTable>
  <tr><td>`.field-wrapper`</td><td>class</td><td>—</td><td>Base field wrapper styles</td></tr>
</PropTable>
