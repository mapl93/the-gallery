## Overview

Empty cart state with CTA to continue shopping.

<Preview html={"<div class=\"cart-empty\" style=\"text-align:center;padding:48px 24px;\"><div class=\"cart-empty__icon\">🛒</div><h2 class=\"cart-empty__title\">Your cart is empty</h2><p class=\"cart-empty__message\">Looks like you haven't added anything yet.</p><a class=\"btn\" href=\"#\">Start Shopping</a></div>"} label="Cart Empty" />

## Usage Guidelines

<DoDont>
  <Do>Show real-time updates (totals, quantities) when the user modifies the cart.</Do>
  <Dont>Don't require a page reload for cart changes — use AJAX for a smooth experience.</Dont>
</DoDont>

## Accessibility

<Callout type="tip">
  Cart components are designed for Shopify's AJAX Cart API. Wire up the JS interactions in your theme.
</Callout>

- Ensure proper color contrast ratios (4.5:1 for text, 3:1 for large text) when customizing tokens.

## Dependencies

This component uses:

- [Empty State](/components/empty-state)
- [Button](/components/button)

## API Reference

<PropTable>
  <tr><td>`.cart-empty`</td><td>class</td><td>—</td><td>Base cart empty styles</td></tr>
</PropTable>
