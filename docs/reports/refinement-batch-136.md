# Refinement Batch 136: Urgency Four-Type Policy Reconciliation

Date: 2026-07-21

Component: `G8` / `urgency`

Status: `human-review-ready` / contract remains `pilot`

## Outcome

The explicit owner response G8-B is now authoritative across ADR 0252, decision
coverage, the packet, contract, open-question ledger, component docs, dossier,
audit and global progress matrix. V1 retains Low Stock, Selling Fast, Viewers
and Recent Sale as passive, text-complete claim types.

Neutral G8 receives only an already-qualified type and complete localized
message. ADR 0252 records the target proof boundary for exact selected-variant
Low Stock, confirmed-purchase velocity, privacy-safe approximate viewer buckets
and anonymous fresh purchase events. Neutral source performs no polling,
calculation, expiry, live announcement or provider work.

The existing implementation and Batch 87 evidence already cover all four
types, so this reconciliation changes no DOM, CSS, renderer or runtime. Shopify
remains `planned` until a real adapter proves its source and lifecycle; copied
CSS alone is not certification. No `stable` promotion is claimed.

## Validation

- contract version `0.4.0`, 183-contract validation and Studio metadata: pass;
- four-type strict omission, direct-container, localized/extreme, contrast,
  forced-color, zero-motion and exact Exhibit/Studio evidence: preserved;
- neutral G8 runtime contribution: `0 B`;
- decision coverage corrected from stale `G8-A` to owner-selected `G8-B`;
- current family/global performance gaps: unchanged and documented; and
- owned evidence resources: clean; `site/dist` not rebuilt.

Preserved evidence is stored under `output/playwright/batch87-urgency/after/`.
