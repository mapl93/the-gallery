## Overview

Native fieldset and legend composition for related canonical form controls.

## Presentation

<Preview html={'<fieldset class="fieldset" aria-describedby="fieldset-description"><legend class="fieldset__legend">Delivery preference</legend><div class="fieldset__description" id="fieldset-description">Choose one option for this order.</div><div class="fieldset__content"><label class="radio"><input class="radio__input" type="radio" name="delivery" value="standard" checked /><span class="radio__label">Standard delivery</span></label><label class="radio"><input class="radio__input" type="radio" name="delivery" value="pickup" /><span class="radio__label">Studio pickup</span></label><label class="radio"><input class="radio__input" type="radio" name="delivery" value="later" /><span class="radio__label">Schedule later</span></label></div></fieldset>'} label="Fieldset" />

## Usage Guidelines

<DoDont>
  <Do>Use Fieldset when controls form one meaningful choice or information group.</Do>
  <Dont>Don't use Fieldset only to draw a decorative border.</Dont>
</DoDont>

## Accessibility

- Preserve native `<fieldset>` and `<legend>` elements.
- Keep the legend visible and concise.
- Associate optional group descriptions with `aria-describedby`.
- Use native `disabled` when the complete group is unavailable.
- Keep controls out of the first legend unless they should remain enabled when the fieldset is disabled.
- Let canonical descendants own names, values, validity, events, and reset.

## References

<PropTable>
  <tr><td>`legend`</td><td>string</td><td>required</td><td>Visible native group label</td></tr>
  <tr><td>`content`</td><td>slot</td><td>required</td><td>Related canonical controls</td></tr>
  <tr><td>`description`</td><td>string</td><td>optional</td><td>Group-level helper text</td></tr>
  <tr><td>`disabled`</td><td>boolean</td><td>`false`</td><td>Disables contained native controls</td></tr>
  <tr><td>`describedBy`</td><td>string</td><td>optional</td><td>Group description id</td></tr>
  <tr><td>`.fieldset`</td><td>class</td><td>—</td><td>Native group root</td></tr>
  <tr><td>`.fieldset__legend`</td><td>class</td><td>—</td><td>Native group label</td></tr>
  <tr><td>`.fieldset__description`</td><td>class</td><td>—</td><td>Optional group helper text</td></tr>
  <tr><td>`.fieldset__content`</td><td>class</td><td>—</td><td>Canonical child-control slot</td></tr>
</PropTable>
