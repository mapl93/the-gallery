## Overview

Right-click context menu. Shares dropdown-menu styles.

<Preview html={"<div class=\"context-menu context-menu--open\" style=\"position:relative;display:inline-block;padding:12px;border-radius:8px;\"><button class=\"dropdown__item\">Copy</button><button class=\"dropdown__item\">Paste</button><div class=\"dropdown__separator\"></div><button class=\"dropdown__item\">Inspect</button></div>"} label="Context Menu" interaction={{"selector":".context-menu","toggle":"context-menu--open","triggerLabel":"Show Menu","startVisible":true}} />

## Usage Guidelines

<DoDont>
  <Do>Use semantic HTML (section, nav, aside) alongside the layout classes.</Do>
  <Dont>Don't nest layout overlays (modal inside drawer, etc.) — it creates confusing z-index stacking.</Dont>
</DoDont>

## Accessibility

<Callout type="warning">
  Layout components handle z-index and focus trapping. Test keyboard navigation before shipping.
</Callout>

- Close on `Escape` key press.
- Use `<nav>` with `aria-label` to identify the navigation region.

## Dependencies

This component uses:

- [Dropdown Menu](/components/dropdown-menu)

## API Reference

<PropTable>
  <tr><td>`.context-menu`</td><td>class</td><td>—</td><td>Base context menu styles</td></tr>
</PropTable>
