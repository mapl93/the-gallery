# Comment Section visual values checkpoint

Date: 2026-09-12. Baseline: `8b406fe`. Decision: ADR 0380.

21 source roles; 41 public values. Five viewports and two themes produce ten
cases and 640 unchanged element geometry/type/color/spacing/border/radius
comparisons. The installed consumer verifies order width, row border, header
and composer gaps plus bounded reply inheritance. A .1 reply share changes
from 40px to 30px when the container changes from 400px to 300px at the same
viewport. Narrow RTL fits; final screenshot inspected.

Studio verifies row border and Body weight editing, independent controlled
reactions, native order request, finite reply flattening, local composer
validation/preview without insertion, reset and Exhibit exposure. No actual
comment was posted or external moderation/authentication provider called.

Ignored `output/playwright/comments-values/` includes fresh CLI provenance,
before CSS, fixture, script, JSON and screenshot. One browser/tab/server;
cleanup/process scan clean and temporary public fixture removed. Source,
catalogue, docs and generated Web/Shopify adapters pass. Local computed-type
comparisons do not certify font delivery, live store behavior or stable maturity.
