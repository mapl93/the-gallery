{
  "$schema": "./contract.schema.json",
  "contractVersion": "0.5.0",
  "slug": "avatar",
  "name": "Avatar",
  "registryId": "A15",
  "category": "primitives",
  "status": "pilot",
  "summary": "Passive circular identity thumbnail with explicit target-resolved native-image or initials composition and four token-customizable size/type defaults.",
  "dependencies": [],
  "source": {
    "registry": "registry.json#/components/avatar",
    "css": {
      "file": "components/css/primitives.css",
      "selector": ".avatar"
    },
    "docs": "site/src/content/components/avatar.mdx"
  },
  "anatomy": [
    {
      "name": "root",
      "selector": ".avatar",
      "className": ".avatar",
      "required": true,
      "description": "Passive fixed-size circular container for one explicit identity composition."
    },
    {
      "name": "image",
      "selector": ".avatar > img",
      "required": false,
      "description": "Optional target-authored native image with context-appropriate alternative text."
    },
    {
      "name": "initials",
      "selector": ".avatar",
      "required": false,
      "description": "Optional target-provided one- or two-grapheme initials text used instead of an image."
    }
  ],
  "variants": [
    {
      "name": "default",
      "className": null,
      "default": true,
      "description": "Neutral circular identity presentation."
    }
  ],
  "sizes": [
    {
      "name": "sm",
      "className": ".avatar--sm",
      "default": false,
      "description": "Small avatar defaults to 32px with Caption initials typography for dense supporting identity contexts."
    },
    {
      "name": "default",
      "className": null,
      "default": true,
      "description": "Default avatar starts at 40px with Body Small initials typography for general identity contexts."
    },
    {
      "name": "lg",
      "className": ".avatar--lg",
      "default": false,
      "description": "Large avatar defaults to 56px with Body Default initials typography for emphasized identity contexts."
    },
    {
      "name": "xl",
      "className": ".avatar--xl",
      "default": false,
      "description": "Extra-large avatar defaults to 80px with Body Large initials typography for prominent identity contexts."
    }
  ],
  "states": [
    {
      "name": "initials",
      "selectors": [".avatar"],
      "description": "Target-provided initials are the explicit rendered identity content."
    },
    {
      "name": "withImage",
      "selectors": [".avatar > img"],
      "description": "A target-authored native image is the explicit rendered identity content."
    }
  ],
  "behavior": [
    {
      "name": "passiveIdentity",
      "selectors": [".avatar"],
      "description": "Avatar has no intrinsic focus, activation, disabled state, status, keyboard model, announcement, or widget role; an actionable identity composes a canonical Link or Button outside it."
    },
    {
      "name": "imageAlternative",
      "selectors": [".avatar > img"],
      "attributes": ["alt"],
      "description": "Native image alt is non-empty when the image uniquely conveys identity and empty when adjacent visible text already names the same entity."
    },
    {
      "name": "initialsAlternative",
      "selectors": [".avatar"],
      "attributes": ["role=\"img\"", "aria-label", "aria-labelledby", "aria-hidden=\"true\""],
      "description": "Standalone initials use one named image role with the full entity name; initials duplicated by adjacent visible identity text are hidden from assistive technology."
    },
    {
      "name": "explicitComposition",
      "selectors": [".avatar", ".avatar > img"],
      "description": "The target explicitly authors either native image or pre-derived initials content. Image loading, failure, delay, responsive source selection, initials derivation, and automatic swapping are not neutral Avatar behavior."
    },
    {
      "name": "fixedGeometry",
      "selectors": [".avatar", ".avatar > img"],
      "description": "The selected logical square does not flex-shrink; native image fills it with centered cover crop; invalid long initials are clipped rather than expanding layout."
    },
    {
      "name": "semanticInitialsScale",
      "selectors": [".avatar", ".avatar--sm", ".avatar--lg", ".avatar--xl"],
      "description": "Each size selects a default diameter and existing semantic initials step: Caption, Body Small, Body Default, and Body Large. Public tokens can customize diameter and typography independently; size remains the only semantic property."
    },
    {
      "name": "forcedColorBoundary",
      "selectors": [".avatar"],
      "description": "Forced colors retains a system-color circular boundary without opting out of the user palette."
    }
  ],
  "properties": [
    {
      "name": "size",
      "type": "enum",
      "required": false,
      "valuesFrom": "sizes",
      "description": "Selects one of four token-customizable diameter/type presets; Default starts at 40px with Body Small initials.",
      "targetMappings": {
        "web": [
          {
            "kind": "optionClass",
            "selector": ".avatar",
            "source": "sizes"
          }
        ]
      }
    }
  ],
  "tokens": {
    "public": {
      "color": [
        "--color-surface-secondary",
        "--color-text-secondary"
      ],
      "radius": [
        "--radius-full"
      ],
      "typography": [
        "--font-family-body",
        "--tg-font-weight-semibold",
        "--typo-caption-size",
        "--typo-body-sm-size",
        "--typo-body-size",
        "--typo-body-lg-size"
      ],
      "spacing": [
        "--space-avatar-small-size",
        "--space-avatar-size",
        "--space-avatar-large-size",
        "--space-avatar-extra-large-size"
      ]
    }
  },
  "accessibility": [
    "Use native img with contextual alt for image composition; do not add a second image role around it.",
    "Give standalone initials one full identity name through aria-label or aria-labelledby; initials alone are not a sufficient accessible name.",
    "Use empty alt or aria-hidden when adjacent visible text already identifies the same person or entity.",
    "Keep Avatar passive and compose a canonical named Link or Button when the identity is actionable.",
    "Do not use avatar color or an unlabeled badge as the only representation of status."
  ],
  "adapters": {
    "web": {
      "status": "implemented",
      "kind": "css",
      "notes": "Canonical CSS implements fixed logical geometry, native image crop, and the accepted semantic initials scale; targets author identity content, contextual names, and image-or-initials fallback selection."
    },
    "shopify": {
      "status": "implemented",
      "kind": "liquid-css-class",
      "notes": "Testimonials embeds the generated class around a Shopify image object and visible adjacent author name. Shopify remains responsible for choosing image or pre-derived initials; neutral Avatar owns no loading/error lifecycle."
    },
    "react": {
      "status": "planned",
      "kind": "component"
    },
    "swiftui": {
      "status": "planned",
      "kind": "style"
    },
    "compose": {
      "status": "planned",
      "kind": "composable"
    }
  }
}
