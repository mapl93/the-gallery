{
  "$schema": "./contract.schema.json",
  "contractVersion": "0.1.0",
  "slug": "share-buttons",
  "name": "Share Buttons",
  "registryId": "L9",
  "category": "blog",
  "status": "pilot",
  "summary": "Social sharing buttons. Inline and sticky sidebar variants.",
  "dependencies": [],
  "source": {
    "registry": "registry.json#/components/share-buttons",
    "css": { "file": "components/css/blog.css", "selector": ".share-buttons" },
    "docs": "site/src/content/components/share-buttons.mdx"
  },
  "anatomy": [
    { "name": "root", "selector": ".share-buttons", "className": ".share-buttons", "required": true, "description": "Share-action composition container." }
  ],
  "variants": [
    { "name": "inline", "className": ".share-buttons--inline", "default": true, "description": "Inline wrapping share-action composition." },
    { "name": "sticky", "className": ".share-buttons--sticky", "default": false, "description": "Column presentation intended for placement by a containing sticky layout." }
  ],
  "sizes": [
    { "name": "default", "className": null, "default": true, "description": "Action sizing remains owned by each composed control." }
  ],
  "states": [
    { "name": "default", "selectors": [".share-buttons"], "description": "Default share-action composition state." }
  ],
  "behavior": [
    { "name": "targetShareActions", "selectors": [".share-buttons"], "description": "The target supplies provider actions, destinations, labels, icons, Web Share behavior, copy feedback, and fallbacks; the canonical share family remains unresolved." }
  ],
  "properties": [
    { "name": "variant", "type": "enum", "required": false, "valuesFrom": "variants", "description": "Inline or column share-action presentation.", "targetMappings": { "web": [{ "kind": "optionClass", "selector": ".share-buttons", "source": "variants" }] } },
    { "name": "actions", "type": "slot", "required": true, "description": "Target-owned share links or buttons with provider-specific behavior and feedback.", "targetMappings": { "web": [{ "kind": "slot", "selector": ".share-buttons", "classNames": [".share-buttons"] }] } }
  ],
  "tokens": { "public": {} },
  "accessibility": [
    "Use links for provider destinations and buttons for actions such as native sharing or copying a link.",
    "Give icon-only actions provider-specific accessible names and preserve each composed control's focus behavior.",
    "Announce copy success or failure through the target action's feedback pattern without changing the component contract here."
  ],
  "adapters": {
    "web": { "status": "implemented", "kind": "css", "notes": "The .share-buttons composition CSS is implemented; provider behavior and reconciliation with .share remain unresolved." },
    "shopify": { "status": "planned", "kind": "liquid-css-js" },
    "react": { "status": "planned", "kind": "component" },
    "figma": { "status": "planned", "kind": "component" },
    "swiftui": { "status": "planned", "kind": "native-component" },
    "compose": { "status": "planned", "kind": "native-component" }
  }
}
