## Overview

Drag-and-drop file upload zone with preview list.

<Preview html={"<div class=\"file-upload\" style=\"max-width:400px;\"><input class=\"file-upload__input\" type=\"file\" id=\"demo-upload\" /><label for=\"demo-upload\"><span class=\"file-upload__icon\">📁</span><span class=\"file-upload__text\">Click to upload or drag and drop</span><span class=\"file-upload__hint\">PNG, JPG up to 10MB</span></label></div>"} label="File Upload / Dropzone" />

## Usage Guidelines

<DoDont>
  <Do>Always pair form inputs with visible labels for accessibility.</Do>
  <Dont>Don't rely on placeholder text as the only label — it disappears on input.</Dont>
</DoDont>

## Accessibility

<Callout type="tip">
  Use the `field-wrapper` component to ensure consistent spacing and label alignment across all form fields.
</Callout>

- Associate every input with a `<label>` using matching `for`/`id` attributes.

## API Reference

<PropTable>
  <tr><td>`.file-upload`</td><td>class</td><td>—</td><td>Base file upload / dropzone styles</td></tr>
</PropTable>
