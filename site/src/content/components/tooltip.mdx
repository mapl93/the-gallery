## Overview

Hover tooltip using aria-label.

<Preview html={"<span class=\"tooltip\"><button class=\"btn btn--outline\">Hover me</button><span class=\"tooltip__content\">Tooltip text</span></span>"} label="Tooltip" />

## Usage Guidelines

<DoDont>
  <Do>Use semantic HTML (section, nav, aside) alongside the layout classes.</Do>
  <Dont>Don't nest layout overlays (modal inside drawer, etc.) — it creates confusing z-index stacking.</Dont>
</DoDont>

## Accessibility

<Callout type="warning">
  Layout components handle z-index and focus trapping. Test keyboard navigation before shipping.
</Callout>

- Ensure proper color contrast ratios (4.5:1 for text, 3:1 for large text) when customizing tokens.

## API Reference

<PropTable>
  <tr><td>`.tooltip`</td><td>class</td><td>—</td><td>Base tooltip styles</td></tr>
</PropTable>
