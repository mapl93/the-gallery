## Overview

Numeric stepper with increment/decrement buttons.

<Preview html={"<div class=\"number-input\" style=\"max-width:200px;\"><label class=\"field__label\">Quantity</label><div style=\"display:flex;align-items:center;gap:8px;\"><button class=\"qty__btn\">−</button><input class=\"qty__input\" type=\"number\" value=\"1\" min=\"0\" /><button class=\"qty__btn\">+</button></div></div>"} label="Number Input" />

## Usage Guidelines

<DoDont>
  <Do>Always pair form inputs with visible labels for accessibility.</Do>
  <Dont>Don't rely on placeholder text as the only label — it disappears on input.</Dont>
</DoDont>

## Accessibility

<Callout type="tip">
  Use the `field-wrapper` component to ensure consistent spacing and label alignment across all form fields.
</Callout>

- Associate every input with a `<label>` using matching `for`/`id` attributes.

## API Reference

<PropTable>
  <tr><td>`.number-input`</td><td>class</td><td>—</td><td>Base number input styles</td></tr>
</PropTable>
