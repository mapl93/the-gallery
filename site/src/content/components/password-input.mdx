## Overview

Password field with show/hide toggle and strength meter.

<Preview html={"<div class=\"password-input\" style=\"max-width:320px;\"><label class=\"field__label\">Password</label><div style=\"position:relative;\"><input class=\"input__field\" type=\"password\" value=\"secret123\" /><button class=\"password-input__toggle\" aria-label=\"Show password\" style=\"position:absolute;right:8px;top:50%;transform:translateY(-50%);\">👁</button></div></div>"} label="Password Input" />

## Usage Guidelines

<DoDont>
  <Do>Always pair form inputs with visible labels for accessibility.</Do>
  <Dont>Don't rely on placeholder text as the only label — it disappears on input.</Dont>
</DoDont>

## Accessibility

<Callout type="tip">
  Use the `field-wrapper` component to ensure consistent spacing and label alignment across all form fields.
</Callout>

- Associate every input with a `<label>` using matching `for`/`id` attributes.

## Dependencies

This component uses:

- [Input](/components/input)

## API Reference

<PropTable>
  <tr><td>`.password-input`</td><td>class</td><td>—</td><td>Base password input styles</td></tr>
</PropTable>
