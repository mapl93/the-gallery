## Overview

Removable tag/chip for filtering.

<Preview html={"<span class=\"tag\">Ceramic <button class=\"tag__remove\" aria-label=\"Remove\">&times;</button></span> <span class=\"tag\">Handmade <button class=\"tag__remove\" aria-label=\"Remove\">&times;</button></span>"} label="Tag" />

## Usage Guidelines

<DoDont>
  <Do>Use this component consistently across your theme for a cohesive UI.</Do>
  <Dont>Don't override the component's built-in states with custom CSS — use the provided variants instead.</Dont>
</DoDont>

## Accessibility

<Callout type="info">
  Primitive components are building blocks. They're referenced by higher-level components throughout the system.
</Callout>

- Ensure proper color contrast ratios (4.5:1 for text, 3:1 for large text) when customizing tokens.

## API Reference

<PropTable>
  <tr><td>`.tag`</td><td>class</td><td>—</td><td>Base tag styles</td></tr>
</PropTable>
