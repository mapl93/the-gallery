## Overview

Groups related Button instances into one visual set without changing the semantics or state of any child.

<Preview html={'<div class="btn-group" role="group" aria-label="Display options"><button class="btn btn--outline" type="button">Option 1</button><button class="btn btn--outline" type="button">Option 2</button><button class="btn btn--outline" type="button">Option 3</button></div>'} label="Button Group" />

## Presentation

Button Group has one inherited Button size and two layout variants. Child variants, labels, disabled states, and activation remain owned by Button.

| Property | Value | Web mapping |
| --- | --- | --- |
| `variant` | `default` | `.btn-group` |
| `variant` | `full` | `.btn-group--full` |
| `groupLabel` | string | `aria-label` on the group |

<Preview html={'<div class="tg-preview-frame tg-preview-frame--md"><div class="btn-group btn-group--full" role="group" aria-label="Display options"><button class="btn btn--outline" type="button">Option 1</button><button class="btn btn--outline" type="button">Option 2</button><button class="btn btn--outline" type="button">Option 3</button></div></div>'} label="Full-width group" />

## Usage Guidelines

<DoDont>
  <Do>Group actions that operate on the same object or represent one compact command set.</Do>
  <Dont>Do not use Button Group to imply a selected value; use Toggle or Segmented Control for selection.</Dont>
</DoDont>

Keep labels concise enough that every action remains legible. In Full, localized or unbroken labels may wrap within their equal share instead of creating horizontal page overflow. Default remains intentionally intrinsic. Use Full only when distributing the available width improves comparison or touch access.

## Accessibility

<Callout type="info">
  Button Group is structural. Every child remains a native Button or Link with its own name and behavior.
</Callout>

- Use `role="group"` with `aria-label` or `aria-labelledby` when nearby text does not already explain the relationship.
- Preserve normal tab order; do not convert the group into one composite focus stop.
- Do not add Toolbar arrow-key navigation; use a distinct Toolbar pattern when one Tab stop and roving focus are required.
- Keep each child action independently named and disabled.
- The focused child is raised above adjacent borders so its focus indicator remains visible.
- Joined borders and outer corners follow logical inline start/end in both LTR and RTL.

## References

<PropTable>
  <tr><td>`groupLabel`</td><td>string</td><td>required</td><td>Accessible name for the related action set</td></tr>
  <tr><td>`variant`</td><td>`default | full`</td><td>`default`</td><td>Content-width or distributed full-width layout</td></tr>
</PropTable>

Button Group depends on [Button](/components/button). Its canonical root is `.btn-group`; child `.btn` instances retain the complete Button contract.

Studio exposes a bounded documentation fixture for changing the number of
buttons and each visible label. Those controls demonstrate child composition;
they do not add a button array, count, or child-label property to the public
Button Group contract.
