## Overview

Determinate bar or circle that represents a known value, plus an indeterminate bar for unknown completion.

<Preview html={'<div class="progress tg-preview-frame tg-preview-frame--md" role="progressbar" aria-label="Upload progress" aria-valuemin="0" aria-valuemax="100" aria-valuenow="65" aria-valuetext="65%"><div class="progress__label"><span class="progress__label-text">Uploading</span><span class="progress__value">65%</span></div><div class="progress__track"><div class="progress__bar tg-preview-w-70"></div></div></div>'} label="Progress" />

## Presentation

Bar and Circle are structural variants. Indeterminate is an orthogonal boolean state available only for Bar and omits `aria-valuenow`.

<Preview html={'<div class="progress-circle" role="progressbar" aria-label="Upload progress" aria-valuemin="0" aria-valuemax="100" aria-valuenow="65" aria-valuetext="65%"><svg viewBox="0 0 36 36" aria-hidden="true"><circle class="progress-circle__bg" cx="18" cy="18" r="15.9"/><circle class="progress-circle__fill" cx="18" cy="18" r="15.9" stroke-dasharray="65 35"/></svg><span class="progress-circle__text">65%</span></div>'} label="Circular progress" />

## Usage Guidelines

<DoDont><Do>Use determinate progress when the target can calculate a meaningful range.</Do><Dont>Do not show a fabricated percentage when completion is unknown.</Dont></DoDont>

Targets own calculations and localized value text. The component owns presentation and progressbar semantics.

## Accessibility

- Provide an accessible operation label and meaningful min/max/value attributes.
- Omit `aria-valuenow` while indeterminate; do not announce every small update.
- Use `aria-valuetext` when a localized phrase is clearer than the raw number.
- Neutral web removes transitions and indeterminate animation under reduced motion.

## References

<PropTable>
  <tr><td>`accessibleLabel`</td><td>string</td><td>required</td><td>Operation name</td></tr>
  <tr><td>`variant`</td><td>`bar | circle`</td><td>`bar`</td><td>Structural presentation</td></tr>
  <tr><td>`value` / `min` / `max`</td><td>number</td><td>conditional / `0` / `100`</td><td>Determinate range</td></tr>
  <tr><td>`label` / `valueText`</td><td>string</td><td>optional</td><td>Visible operation and localized value text</td></tr>
  <tr><td>`indeterminate`</td><td>boolean</td><td>`false`</td><td>Unknown-duration Bar state</td></tr>
</PropTable>
