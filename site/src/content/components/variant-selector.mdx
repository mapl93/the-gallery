## Overview

Swatches and pills for product variant selection.

<Preview html={"<div class=\"variant-selector\"><span class=\"variant-selector__label\">Size</span><div class=\"variant-pills\"><label class=\"variant-pill\"><input type=\"radio\" name=\"size\" value=\"s\" />S</label><label class=\"variant-pill\"><input type=\"radio\" name=\"size\" value=\"m\" checked />M</label><label class=\"variant-pill\"><input type=\"radio\" name=\"size\" value=\"l\" />L</label><span class=\"variant-pill variant-pill--unavailable\">XL</span></div></div>"} label="Variant Selector" />

## Presentation

Swatches and pills are composition choices for option groups. Native checked, disabled, focus, and unavailable states remain synchronized with visible labels.

## Usage Guidelines

<DoDont>
  <Do>Optimize product images for web — use WebP format and responsive srcset.</Do>
  <Dont>Don't overload product components with too many badges or CTAs — keep it clean.</Dont>
</DoDont>

## Accessibility

<Callout type="tip">
  Product components are designed to work together. The product page combines gallery, info, form, and related products.
</Callout>

- Associate every input with a `<label>` using matching `for`/`id` attributes.
- Use radio semantics for mutually exclusive variant choices.
- Keep unavailable states synchronized with `disabled` or `aria-disabled`.

## References

<PropTable>
  <tr><td>`.variant-selector`</td><td>class</td><td>—</td><td>Base variant selector styles</td></tr>
  <tr><td>`.variant-selector__group`</td><td>class</td><td>—</td><td>Single option group</td></tr>
  <tr><td>`.variant-selector__label`</td><td>class</td><td>—</td><td>Option group label</td></tr>
  <tr><td>`.variant-swatches`</td><td>class</td><td>—</td><td>Swatch option group</td></tr>
  <tr><td>`.variant-swatch`</td><td>class</td><td>—</td><td>Single swatch option</td></tr>
  <tr><td>`.variant-swatch--image`</td><td>modifier</td><td>—</td><td>Image-backed swatch</td></tr>
  <tr><td>`.variant-swatch--unavailable`</td><td>state</td><td>—</td><td>Unavailable swatch</td></tr>
  <tr><td>`.variant-swatch__color`</td><td>class</td><td>—</td><td>Swatch color or image surface</td></tr>
  <tr><td>`.variant-swatch__ring`</td><td>class</td><td>—</td><td>Selected or focus ring layer</td></tr>
  <tr><td>`.variant-pills`</td><td>class</td><td>—</td><td>Pill option group</td></tr>
  <tr><td>`.variant-pill`</td><td>class</td><td>—</td><td>Single pill option</td></tr>
  <tr><td>`.variant-pill--unavailable`</td><td>state</td><td>—</td><td>Unavailable pill option</td></tr>
</PropTable>
