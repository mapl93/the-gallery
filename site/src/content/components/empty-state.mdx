## Overview

Placeholder for empty collections, carts, search results.

<Preview html={"<div class=\"empty-state\" style=\"max-width:400px;\"><div class=\"empty-state__icon\">📦</div><h3 class=\"empty-state__heading\">Nothing here yet</h3><p class=\"empty-state__text\">Start browsing to discover unique artisan pieces.</p><a class=\"empty-state__cta btn\" href=\"#\">Explore</a></div>"} label="Empty State" />

## Usage Guidelines

<DoDont>
  <Do>Use this component consistently across your theme for a cohesive UI.</Do>
  <Dont>Don't override the component's built-in states with custom CSS — use the provided variants instead.</Dont>
</DoDont>

## Accessibility

<Callout type="info">
  Primitive components are building blocks. They're referenced by higher-level components throughout the system.
</Callout>

- Ensure proper color contrast ratios (4.5:1 for text, 3:1 for large text) when customizing tokens.

## API Reference

<PropTable>
  <tr><td>`.empty-state`</td><td>class</td><td>—</td><td>Base empty state styles</td></tr>
</PropTable>
