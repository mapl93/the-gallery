## Overview

Shimmer loading placeholder.

<Preview html={"<div class=\"skeleton\" style=\"width:200px;height:16px;margin-bottom:8px;\"></div><div class=\"skeleton\" style=\"width:160px;height:16px;margin-bottom:8px;\"></div><div class=\"skeleton skeleton--circle\" style=\"width:48px;height:48px;\"></div>"} label="Loading Skeleton" />

## Usage Guidelines

<DoDont>
  <Do>Use this component consistently across your theme for a cohesive UI.</Do>
  <Dont>Don't override the component's built-in states with custom CSS — use the provided variants instead.</Dont>
</DoDont>

## Accessibility

<Callout type="info">
  Primitive components are building blocks. They're referenced by higher-level components throughout the system.
</Callout>

- Use `aria-live="polite"` or `role="status"` so screen readers announce loading state.

## API Reference

<PropTable>
  <tr><td>`.skeleton`</td><td>class</td><td>—</td><td>Base loading skeleton styles</td></tr>
</PropTable>
