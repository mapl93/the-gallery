## Overview

Masonry Gallery presents a passive, source-ordered artwork list in intrinsic columns (up to three by default), preserving natural media proportions and optional persistent captions.

<Preview
  html={'<ul class="masonry-gallery tg-preview-frame tg-preview-frame--lg" aria-label="Selected ceramic works"><li class="masonry-gallery__item"><figure class="gallery-piece"><img class="tg-preview-media tg-preview-media--portrait" src="data:image/gif;base64,R0lGODlhAQABAAAAACw=" alt="" /><figcaption class="gallery-piece__overlay"><span class="gallery-piece__title">Celadon Study</span><span class="gallery-piece__price">$420</span></figcaption></figure></li><li class="masonry-gallery__item"><figure class="gallery-piece"><img class="tg-preview-media tg-preview-media--landscape" src="data:image/gif;base64,R0lGODlhAQABAAAAACw=" alt="" /><figcaption class="gallery-piece__overlay"><span class="gallery-piece__title">River Bowls</span><span class="gallery-piece__price">$280</span></figcaption></figure></li><li class="masonry-gallery__item"><figure class="gallery-piece"><img class="tg-preview-media tg-preview-media--square" src="data:image/gif;base64,R0lGODlhAQABAAAAACw=" alt="" /><figcaption class="gallery-piece__overlay"><span class="gallery-piece__title">Basalt Form</span><span class="gallery-piece__price">$360</span></figcaption></figure></li></ul>'}
  label="Masonry Gallery"
/>

## Presentation

The required `items` slot contains native ordered `.masonry-gallery__item` records. Each record requires a `.gallery-piece` and image; caption, title, and price regions are optional. The gallery grows from one to its token-defined maximum (three initially) from its own available inline size, without viewport breakpoints or JavaScript measurement.

The canonical candidate intentionally uses passive figures and persistent caption metadata on every input. ADR 0080 leaves activation unresolved: a target may later choose passive pieces, native links, Lightbox triggers, or more than one target-specific mode. Only semantically valid target-native interaction may opt into paired fine-pointer hover and focus-visible disclosure.

Preferred column width, maximum column count, vertical item gap, caption
padding/title-price gap, focus and optional interactive media scale are public
tokens. Defaults remain 13rem and a maximum of three columns; the browser
chooses the actual count from available space. Counts are positive integers
with a lower bound of one and no neutral upper limit. The column width is a
preferred measure rather than a guaranteed minimum when space is insufficient.
Natural image proportions and source order remain target-owned. Passive and
disabled native pieces keep captions visible and have no hover emphasis.

Copy-and-own consumers explicitly adopt CSS/tokens and corresponding Studio
metadata. No semantic properties, activation modes or runtime are added.

## Usage Guidelines

<DoDont>
  <Do>Use native list/figure markup, keep artwork in a meaningful source order, and let target-owned natural dimensions shape the columns.</Do>
  <Dont>Do not assume that every gallery piece opens a link or Lightbox.</Dont>
</DoDont>

- Use concise overlay metadata that does not replace the image description.
- Choose title and price content according to target data policy.
- Omit an empty caption, title, or price instead of rendering blank anatomy.
- Add native interactive markup only after the target activation mode is decided.

## Accessibility

- Give every meaningful artwork image useful alternative text and mark decorative images accordingly.
- Keep passive caption title and price visibly available without hover-only interaction.
- If a target makes a piece interactive, use native link or button semantics with an accessible name and visible focus.
- Preserve source order across intrinsic column changes; do not use dense packing, CSS order, or image scale alone to communicate activation.

## References

**Dependencies:** None. Lightbox is not a dependency while activation remains unresolved.

**Anatomy:** required native `.masonry-gallery` list, `.masonry-gallery__item` list item, passive `.gallery-piece` figure, and piece image; optional `.gallery-piece__overlay` caption, `.gallery-piece__title`, and `.gallery-piece__price`.

**API Reference**

<PropTable>
  <tr><td>`items`</td><td>slot</td><td>required</td><td>Ordered target-owned artwork items and optional metadata</td></tr>
</PropTable>
