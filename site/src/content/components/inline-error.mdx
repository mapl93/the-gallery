## Overview

Text-first validation error message associated with an invalid control.

## Presentation

<Preview html={'<div class="inline-error"><svg class="inline-error__icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true" focusable="false"><circle cx="12" cy="12" r="10"></circle><line x1="12" x2="12" y1="8" y2="12"></line><line x1="12" x2="12.01" y1="16" y2="16"></line></svg><span class="inline-error__message">Enter a valid email address.</span></div>'} label="Inline Error" />

## Usage Guidelines

<DoDont>
  <Do>Explain what failed and how the user can correct it.</Do>
  <Dont>Don't rely on the icon or error color as the only message.</Dont>
</DoDont>

## Accessibility

- Associate the message with its invalid control through `aria-describedby`.
- Set `aria-invalid="true"` on the control, not the message.
- Keep static associated errors at announcement `none`.
- Use `polite` for routine dynamic validation and `assertive` only when interruption is necessary.
- Hide decorative icons from assistive technology.

## References

<PropTable>
  <tr><td>`message`</td><td>string</td><td>required</td><td>Visible error explanation</td></tr>
  <tr><td>`icon`</td><td>slot</td><td>optional</td><td>Decorative supporting icon</td></tr>
  <tr><td>`announcement`</td><td>enum</td><td>`none`</td><td>`none`, `polite`, or `assertive`</td></tr>
  <tr><td>`.inline-error`</td><td>class</td><td>—</td><td>Error message root</td></tr>
  <tr><td>`.inline-error__icon`</td><td>class</td><td>—</td><td>Optional decorative icon</td></tr>
  <tr><td>`.inline-error__message`</td><td>class</td><td>—</td><td>Required explanatory text</td></tr>
</PropTable>
