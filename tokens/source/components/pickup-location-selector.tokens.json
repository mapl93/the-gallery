{
  "$description": "pickup-location-selector public visual roles. Location Search is shared anatomy under ADR 0244, not a separate registry component.",
  "component": {
    "pickup-location-selector": {
      "selectedRingWidth": {
        "$type": "dimension",
        "$value": "{dimension.1}"
      },
      "detailsOffset": {
        "$type": "dimension",
        "$value": "{dimension.0}"
      },
      "detailsInset": {
        "$type": "dimension",
        "$value": "{dimension.0}"
      }
    }
  }
}
