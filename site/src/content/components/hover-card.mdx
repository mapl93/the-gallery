## Overview

Content preview on hover. For product previews, user cards.

<Preview html={"<div class=\"hover-card\" style=\"position:relative;display:inline-block;\"><a href=\"#\">Hover me</a><div class=\"hover-card__content\" style=\"position:relative;margin-top:8px;padding:16px;\"><p>Hover card content with details.</p></div></div>"} label="Hover Card" />

## Usage Guidelines

<DoDont>
  <Do>Use semantic HTML (section, nav, aside) alongside the layout classes.</Do>
  <Dont>Don't nest layout overlays (modal inside drawer, etc.) — it creates confusing z-index stacking.</Dont>
</DoDont>

## Accessibility

<Callout type="warning">
  Layout components handle z-index and focus trapping. Test keyboard navigation before shipping.
</Callout>

- All images must have descriptive `alt` text.

## API Reference

<PropTable>
  <tr><td>`.hover-card`</td><td>class</td><td>—</td><td>Base hover card styles</td></tr>
</PropTable>
