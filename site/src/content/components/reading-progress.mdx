## Overview

Fixed top bar showing scroll progress through article.

<Preview html={"<div class=\"reading-progress\" style=\"position:relative;height:4px;background:var(--color-surface-secondary);border-radius:2px;\"><div style=\"width:35%;height:100%;background:var(--color-text-accent);border-radius:2px;\"></div></div>"} label="Reading Progress Bar" />

## Usage Guidelines

<DoDont>
  <Do>Use the article body/prose component for rich text — it handles typography, images, and spacing.</Do>
  <Dont>Don't manually style article content with inline styles — let the prose class handle it.</Dont>
</DoDont>

## Accessibility

<Callout type="info">
  Blog components support Shopify's article template system. Use metafields for custom article data.
</Callout>

- Use `aria-live="polite"` or `role="status"` so screen readers announce loading state.

## API Reference

<PropTable>
  <tr><td>`.reading-progress`</td><td>class</td><td>—</td><td>Base reading progress bar styles</td></tr>
</PropTable>
