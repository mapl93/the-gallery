## Overview

One-time code input with individual character cells.

<Preview html={"<div class=\"pin-input\"><input class=\"pin-input__field pin-input__field--filled\" type=\"text\" maxlength=\"1\" value=\"4\" /><input class=\"pin-input__field pin-input__field--filled\" type=\"text\" maxlength=\"1\" value=\"2\" /><input class=\"pin-input__field\" type=\"text\" maxlength=\"1\" /><input class=\"pin-input__field\" type=\"text\" maxlength=\"1\" /></div>"} label="Pin Input / OTP" />

## Usage Guidelines

<DoDont>
  <Do>Always pair form inputs with visible labels for accessibility.</Do>
  <Dont>Don't rely on placeholder text as the only label — it disappears on input.</Dont>
</DoDont>

## Accessibility

<Callout type="tip">
  Use the `field-wrapper` component to ensure consistent spacing and label alignment across all form fields.
</Callout>

- Associate every input with a `<label>` using matching `for`/`id` attributes.

## API Reference

<PropTable>
  <tr><td>`.pin-input`</td><td>class</td><td>—</td><td>Base pin input / otp styles</td></tr>
</PropTable>
