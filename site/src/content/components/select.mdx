## Overview

Dropdown select with label.

<Preview html={"<div class=\"select\" style=\"max-width:320px;\"><label class=\"select__label\">Country</label><select class=\"select__field\"><option>Choose an option</option><option>Argentina</option><option>Mexico</option><option>Spain</option></select></div>"} label="Select" />

## Usage Guidelines

<DoDont>
  <Do>Use this component consistently across your theme for a cohesive UI.</Do>
  <Dont>Don't override the component's built-in states with custom CSS — use the provided variants instead.</Dont>
</DoDont>

## Accessibility

<Callout type="info">
  Primitive components are building blocks. They're referenced by higher-level components throughout the system.
</Callout>

- Associate every input with a `<label>` using matching `for`/`id` attributes.

## API Reference

<PropTable>
  <tr><td>`.select`</td><td>class</td><td>—</td><td>Base select styles</td></tr>
</PropTable>
