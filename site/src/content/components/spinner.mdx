## Overview

CSS-only loading spinner. sm, lg, xl sizes. Optional overlay.

<Preview html={"<span class=\"spinner\"></span> <span class=\"spinner spinner--lg\"></span>"} label="Spinner" />

## Sizes

<Preview html={"<span class=\"spinner spinner--sm\"></span> <span class=\"spinner\"></span> <span class=\"spinner spinner--lg\"></span> <span class=\"spinner spinner--xl\"></span>"} label="Sizes" />

| Class | Size |
|-------|------|
| `.spinner--sm` | Sm |
| *(default)* | Default |
| `.spinner--lg` | Lg |
| `.spinner--xl` | Xl |

## Usage Guidelines

<DoDont>
  <Do>Use this component consistently across your theme for a cohesive UI.</Do>
  <Dont>Don't override the component's built-in states with custom CSS — use the provided variants instead.</Dont>
</DoDont>

## Accessibility

<Callout type="info">
  Primitive components are building blocks. They're referenced by higher-level components throughout the system.
</Callout>

- Associate every input with a `<label>` using matching `for`/`id` attributes.
- Use `aria-live="polite"` or `role="status"` so screen readers announce loading state.

## API Reference

<PropTable>
  <tr><td>`.spinner`</td><td>class</td><td>—</td><td>Base spinner styles</td></tr>
  <tr><td>`.spinner--sm`</td><td>modifier</td><td>—</td><td>Sm size</td></tr>
  <tr><td>`.spinner--lg`</td><td>modifier</td><td>—</td><td>Lg size</td></tr>
  <tr><td>`.spinner--xl`</td><td>modifier</td><td>—</td><td>Xl size</td></tr>
</PropTable>
