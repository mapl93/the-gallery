## Overview

Expandable promo code input with applied state.

<Preview html={"<div class=\"discount-field\" style=\"max-width:360px;\"><button class=\"discount-field__toggle\">Have a discount code?</button><div class=\"discount-field__form\" style=\"display:flex;gap:8px;margin-top:8px;\"><input class=\"input__field\" type=\"text\" placeholder=\"Enter code\" style=\"flex:1;\" /><button class=\"btn btn--outline\">Apply</button></div></div>"} label="Discount / Promo Field" />

## Usage Guidelines

<DoDont>
  <Do>Show real-time updates (totals, quantities) when the user modifies the cart.</Do>
  <Dont>Don't require a page reload for cart changes — use AJAX for a smooth experience.</Dont>
</DoDont>

## Accessibility

<Callout type="tip">
  Cart components are designed for Shopify's AJAX Cart API. Wire up the JS interactions in your theme.
</Callout>

- Associate every input with a `<label>` using matching `for`/`id` attributes.

## Dependencies

This component uses:

- [Input](/components/input)
- [Button](/components/button)

## API Reference

<PropTable>
  <tr><td>`.discount-field`</td><td>class</td><td>—</td><td>Base discount / promo field styles</td></tr>
</PropTable>
