## Overview

Increment/decrement quantity control.

<Preview html={"<div class=\"qty\"><button class=\"qty__btn\" aria-label=\"Decrease\">−</button><input class=\"qty__input\" type=\"number\" value=\"1\" min=\"1\" /><button class=\"qty__btn\" aria-label=\"Increase\">+</button></div>"} label="Quantity Selector" />

## Usage Guidelines

<DoDont>
  <Do>Use this component consistently across your theme for a cohesive UI.</Do>
  <Dont>Don't override the component's built-in states with custom CSS — use the provided variants instead.</Dont>
</DoDont>

## Accessibility

<Callout type="info">
  Primitive components are building blocks. They're referenced by higher-level components throughout the system.
</Callout>

- Associate every input with a `<label>` using matching `for`/`id` attributes.

## API Reference

<PropTable>
  <tr><td>`.qty`</td><td>class</td><td>—</td><td>Base quantity selector styles</td></tr>
</PropTable>
