# Refinement Batch 135: Newsletter Owner-Decision Reconciliation

Date: 2026-07-21

Component: `G2` / `newsletter`

Status: `human-review-ready` / contract remains `pilot`

## Outcome

Owner-selected G2-A is reconciled across ADR 0251, the open-question ledger,
component documentation, dossier, audit and global progress matrix. Newsletter
remains a thin titled composition around canonical native Form, Input and
Button with one optional target-confirmed response placement.

Neutral G2 defines no pending, success, error, retry, duplicate, provider or
consent state machine. The target owns submission, persistence, confirmed
outcomes, retry and resulting focus/announcement behavior. Shopify continues to
use its native customer form and server-confirmed results; the docs fixture
truthfully states that no subscription was sent.

The accepted decision matches the existing implementation and Batch 83
evidence, so no DOM, CSS, renderer, runtime or browser regeneration is needed.
Final visual approval, live target/provider proof and G2-specific design
evidence remain gates. No `stable` promotion is claimed.

## Validation

- existing four-viewport, direct-container, native validation, keyboard,
  localized RTL/effective-200%, contrast, forced-color, reduced-motion and
  exact Exhibit/Studio parity evidence: preserved;
- neutral G2 runtime contribution: `0 B`;
- current Marketing/global performance gaps: unchanged and documented;
- component contracts, Studio metadata, docs, adapters and refinement matrix:
  revalidated; and
- owned evidence resources: clean; `site/dist` not rebuilt.

Preserved evidence is stored under `output/playwright/batch83-newsletter/after/`.
